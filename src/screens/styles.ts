import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  app_container: {
    flex: 1,
    padding: 30,
    backgroundColor: '#FFF0F9',
  },
  under_logo_text: {
    alignItems: 'center',
    gap: 10,
    marginBottom: 15,
  },
});
