import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  socialMedia_block: {
    gap: 16,
    marginBottom: 25,
  },
  socialMedia_row: {
    flexDirection: 'row',
    minWidth: '100%',
    gap: 16,
  },
  apple_additional_style: {
    width: 17,
    height: 20,
  },
});
